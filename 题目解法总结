1、子集问题：1、可以使用回溯算法， 2、可以使用位运算的思想：每个元素有两种状态：选择或者不选，因此有n^2中状态
2、排列、可以使用桶的位置思路：每个元素都可以放在位置1，然后再把剩下的元素进行算则
3、k个链表的合并问题；1、使用优先级队列，2、使用归并排序模板+合并
4、链表的在构造问题：奇数在前偶数在后、小于某个数的在前，大于的在后、等等可以使用双指针算法，再次构造两个链表
5、删除链表中的重复元素 1、递归删除法，2迭代删除，构造头结点，找到下一个不同的元素
